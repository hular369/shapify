{
    "name": "hular369/geo-shapify",
    "description": "A PHP library that allows to check if a coordinate(lat,long) is inside or outside of a circle or a polygon",
    "type": "library",
    "require": {
        "php": ">=7.0"
    },
    "autoload": {
        "psr-4": {
            "Hular369\\GeoShapify\\": "src/"
        }
    }
}