{
    "name": "hular369/geo-shapify",
    "description": "A PHP library that allows to check if a coordinate(lat,long) is inside or outside of a circle or a polygon, calculate area occupied by a polygon/circle, and find out the nearest vertex of a polygon from a given point.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=7.0"
    },
    "autoload": {
        "psr-4": {
            "Hular369\\GeoShapify\\": "src/"
        }
    }
}